sudo: false

language: node_js

node_js:
  - 6
  - 5
  - 4
  - '0.12'

before_install:
  sh -c 'npm install phantomjs-prebuilt@$PHANTOMJS || npm install phantomjs@$PHANTOMJS'

env:
  - PHANTOMJS=1
  - PHANTOMJS=2
